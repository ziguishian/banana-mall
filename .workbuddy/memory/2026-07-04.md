# MxPage 项目日志 - 2026-07-04

## 启动系统
- 节点在运行，已打开预览面板

## 打包 MxPage V4 生产版本
- 执行 `npm run build` 生产构建，输出 `.next/standalone`（约100MB）
- 复制 `.env`、`prisma/`、`public/` 到 standalone
- 打包为 `mxpage-v4-standalone.zip`（64MB），准备上传云服务部署

## SaaS 改造方案分析（截图）
- 方案：中英双语 + 登录注册 + 会员次数充值
- 技术选型合理：Better Auth / Next-Intl / PostgreSQL / 支付
- 可行但改造量大，建议渐进式改造

## SaaS 改造第一阶段落地（登录 + 次数扣费）
用户确认：自建轻量认证 / Key 两者支持 / 注册送20每次扣1。已完成并 build 通过：
- schema 加 User/CreditTransaction/UserProviderConfig + Project.userId（迁移 20260704000001_sa_auth）
- lib/auth（password/jwt/session）+ /api/auth/{register,login,logout,me}
- middleware.ts 守卫（除 login/register/api/auth 外全拦截）
- 登录/注册页 + AppShell 登录页跳过 shell
- lib/credits（service + guard：withCreditGuard/withCreditGuardAsync）接入 10 个 AI route
- UserMenu（余额+登出）+ /settings/my-keys 个人 key 管理页 + /api/user/providers CRUD
- saveProviderConfig 改为存真实 apiKey（旧版存空），平台 key 才能从 DB 解密
- listProjects/createProject 加可选 userId 隔离
- tsc 零错误，npm run build 成功（路由表含 /login /register /settings/my-keys /api/user/providers + Middleware 32.6kB）
- 详细架构见 MEMORY.md

## SaaS 改造第二阶段落地（权限/退费/i18n/支付）
用户全选 5 项，全部完成并 build 通过：
1. 权限加固：getProjectDetail/updateProject 加 userId 校验抛 403；/api/providers POST/PATCH 限管理员；/api/auth/bootstrap-admin 一次性自举
2. 异步任务失败退费：withCreditGuardAsync handler 加 ctx{userId,chargedCredits}；workflow-task-service 在 failTask 前退费；retry 不重复扣费
3. i18n 无路由前缀：lib/i18n(types/dictionaries/store) + LocaleSwitcher + 接入 AppShell/AuthForm/my-keys/credits
4. Payjs 支付：schema 加 Order（迁移 20260704000002_payment）；lib/payments/payjs.ts；/api/credits/{recharge,orders,payjs-notify}；/settings/credits 充值页（套餐+二维码+3秒轮询+记录表）
- env 加 PAYJS_*/CREDITS_PER_YUAN/APP_BASE_URL（zod 允许空字符串 URL）
- 修了 env zod url 校验拒绝空字符串的坑（.or(z.literal(""))）
- 详细架构见 MEMORY.md
